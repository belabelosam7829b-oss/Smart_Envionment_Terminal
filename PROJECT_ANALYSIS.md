# STM32 智能环境采集终端 — 项目分析报告

> 生成日期：2026-07-23

---

## 目录

1. [项目整体功能](#1-项目整体功能)
2. [系统框图](#2-系统框图)
3. [每个模块的作用](#3-每个模块的作用)
4. [STM32 各外设配置](#4-stm32-各外设配置)
5. [各驱动之间的关系](#5-各驱动之间的关系)
6. [数据流向](#6-数据流向)
7. [软件架构](#7-软件架构)
8. [项目亮点](#8-项目亮点)

---

## 1. 项目整体功能

本项目是一个**STM32 + Python 双端协作的智能环境监测系统**：

| 端 | 职责 | 核心功能 |
|----|------|---------|
| **STM32 终端** | 传感器数据采集、就地显示、数据上报 | 采集温湿度(DHT11)、光照(5516)，驱动 OLED 屏显示，通过 USART 串口上报 |
| **Python 上位机** | 数据可视化、持久化存储 | 串口读取传感器数据，matplotlib 实时绘图，CSV 文件日志存储 |

**一句话总结**：STM32 作为下位机采集环境参数，同时在本地 OLED 显示并通过串口上报；Python 作为上位机接收并实时绘图、存档。

---

## 2. 系统框图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      STM32F103C8T6 (72MHz)                         │
│                                                                     │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌───────────────┐   │
│  │ DHT11    │   │ 5516     │   │ OLED     │   │ USART3        │   │
│  │ (PA9)    │   │ (PA0)    │   │ (PB8/PB9)│   │ (PB10/PB11)   │   │
│  │ 温湿度    │   │ 光照强度  │   │ 本地显示  │   │ 115200-8N1    │   │
│  └────┬─────┘   └────┬─────┘   └────┬─────┘   └───────┬───────┘   │
│       │              │              │                  │           │
│       ▼              ▼              ▼                  ▼           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │               main.c 主循环（1秒周期）                       │   │
│  │  LED翻转 → 清屏 → 传感器读取 → OLED显示 → 串口输出 → 刷屏  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└──────────────────────────────────────────────────┬──────────────────┘
                                                    │ UART (串口线)
                                                    ▼
┌────────────────────────────────────────────────────────────────────┐
│                      Python 上位机 (PC)                            │
│                                                                    │
│  ┌─────────────┐    ┌─────────────┐    ┌──────────────────────┐   │
│  │ pyserial     │    │ matplotlib  │    │ CSV File             │   │
│  │ 串口读取     │───▶│ 实时3子图   │    │ date/sensor_data.csv │   │
│  │ 解析 T/H/L  │    │ 50点滚动    │    │ PC时间戳存档          │   │
│  └─────────────┘    └─────────────┘    └──────────────────────┘   │
│                                                                    │
│  支持: 命令行指定COM口 | .venv 隔离 | monitor.exe 免环境打包       │
└────────────────────────────────────────────────────────────────────┘
```

---

## 3. 每个模块的作用

### 3.1 STM32 端模块

#### (1) 温湿度传感器 — DHT11

| 项目 | 内容 |
|------|------|
| **接口** | PA9 单总线 (1-Wire)，GPIO 软件模拟时序 |
| **通信流程** | 主机拉低 ≥18ms → 拉高 20-40μs → 从机响应 → 40bit数据（5字节） |
| **数据格式** | 湿度整数(8bit) + 湿度小数(8bit) + 温度整数(8bit) + 温度小数(8bit) + 校验和(8bit) |
| **精度** | 温度 ±2°C，湿度 ±5% RH |
| **采样周期** | ≥1 秒（推荐） |
| **校验** | 校验和 = (byte0 + byte1 + byte2 + byte3) & 0xFF |
| **源文件** | `Drivers/User/Src/dht11.c`，`Drivers/User/Inc/dht11.h` |

#### (2) 光照传感器 — 5516 光敏电阻

| 项目 | 内容 |
|------|------|
| **接口** | PA0 (ADC1_IN0) 模拟输入 |
| **ADC 配置** | 12位分辨率，连续转换模式，软件触发 |
| **采样时间** | 239.5 个 ADC 时钟周期 |
| **映射逻辑** | 原始值 0-4095 → 百分比 0-100 |
| **反向映射** | 因为电路设计手遮挡值变高，所以 `percentage = 100 - (raw * 100 / 4095)` |
| **源文件** | `Drivers/User/Src/5516.c`，`Drivers/User/Inc/5516.h` |

#### (3) OLED 显示屏 — SSD1306 128×64

| 项目 | 内容 |
|------|------|
| **接口** | PB8 (SCL) / PB9 (SDA)，I2C1 |
| **I2C 速度** | 100kHz 标准模式 |
| **设备地址** | 自动检测（0x78 或 0x7A） |
| **显示方式** | 显存缓冲区 (128×8 bytes)，整帧 I2C 刷新 |
| **字库** | 内嵌 8×16 ASCII 字库（95 个字符），覆盖可打印 ASCII |
| **布局** | 4 行：LED 状态(第0行) / 温度(第16行) / 湿度(第32行) / 光照(第48行) |
| **源文件** | `Drivers/User/Src/OLED.c`，`Drivers/User/Inc/OLED.h` |

#### (4) LED 指示灯

| 项目 | 内容 |
|------|------|
| **引脚** | PA1，推挽输出 |
| **状态** | 主循环每秒翻转一次 |
| **源文件** | `Drivers/User/Src/led.c`，`Drivers/User/Inc/led.h` |

#### (5) 串口通信 — USART3

| 项目 | 内容 |
|------|------|
| **引脚** | PB10 (TX) / PB11 (RX) |
| **参数** | 115200 baud, 8数据位, 1停止位, 无校验, 无硬件流控 |
| **重定向** | 同时支持 GCC `_write()` 和 ARMCC `fputc()`，兼容全部工具链 |
| **超时保护** | 100ms 超时，避免阻塞主循环 |
| **输出格式** | `[YYYY-MM-DD HH:MM:SS] T:24C, H:55%, L:78%` |
| **源文件** | `Drivers/User/Src/Serial.c`，`Drivers/User/Inc/Serial.h` |

#### (6) RTC 实时时钟

| 项目 | 内容 |
|------|------|
| **时钟源** | LSI (~40kHz 内部低速振荡器) |
| **格式** | BCD 编码 |
| **首次初始化** | 备份寄存器 `RTC_BKP_DR1` 写入 0xA5A5 标记，下次重启跳过设时 |
| **精度** | 受 LSI 温漂影响 (±5% 典型)，需 NTP 或手动校准 |
| **源文件** | `Core/Src/rtc.c`，`Core/Inc/rtc.h` |

#### (7) TIM2 — 微秒级定时器

| 项目 | 内容 |
|------|------|
| **预分频** | 71 (72MHz / 72 = 1MHz = 1μs 分辨率) |
| **自动重装** | 65535 (最大 65535μs 延时) |
| **用途** | 为 DHT11 驱动提供精确微秒级延时 |
| **源文件** | `Core/Src/tim.c`，`Core/Inc/tim.h` |

### 3.2 Python 端模块

| 模块 | 作用 | 技术实现 |
|------|------|---------|
| **serial (pyserial)** | 串口通信 | `serial.Serial(COM9, 115200, timeout=1)` |
| **matplotlib** | 实时可视化 | `FuncAnimation` + 3 子图（温度/湿度/光照），50点滚动 |
| **csv** | 数据记录 | `date/sensor_data.csv`，追加模式，PC系统时间戳 |
| **PyInstaller** | 打包分发 | `monitor.exe` 无窗口版，依赖项打包，无需 Python 环境 |

---

## 4. STM32 各外设配置

### 4.1 时钟树

```
HSE (8MHz 外部晶振)
  └── PLL (x9) ──→ SYSCLK (72MHz)
                      ├── AHB (HCLK) = 72MHz
                      │     ├── APB1 (PCLK1) = 36MHz  ← USART3, I2C1, TIM2
                      │     └── APB2 (PCLK2) = 72MHz  ← ADC1, GPIO
                      └── ADC 时钟 = APB2/6 = 12MHz

LSI (~40kHz)
  └── RTC 时钟源
```

### 4.2 资源配置总表

| 外设 | 引脚 | 功能 | 配置参数 |
|------|------|------|---------|
| GPIOA | PA0 | **ADC1_IN0** (5516 光敏) | 模拟输入 |
| GPIOA | PA1 | **LED** (输出) | 推挽输出，低频 |
| GPIOA | PA9 | **DHT11** (单总线) | 推挽输出/输入，高速 |
| I2C1 | PB8 (SCL) | **OLED** | 100kHz，7位地址 |
| I2C1 | PB9 (SDA) | **OLED** | 复用开漏，高速 |
| USART3 | PB10 (TX) | **串口发送** | 复用推挽，高速 |
| USART3 | PB11 (RX) | **串口接收** | 浮空输入 |
| TIM2 | — | **μs 延时发生器** | 72分频，65535周期 |
| RTC | — | **实时时钟** | LSI 时钟，BCD 格式 |

### 4.3 FLASH / RAM 占用

```
RAM:   3,328 B / 20 KB  (16.25%)
FLASH: 26,496 B / 64 KB (40.43%)
```

Flash 余量充足 (60%)，RAM 余量充足 (84%)，代码有较大扩展空间。

---

## 5. 各驱动之间的关系

```
                            ┌───────────┐
                            │   TIM2    │  提供微秒级延时（DHT11 时序关键依赖）
                            └─────┬─────┘
                                  │ 依赖
                                  ▼
     ┌───────────┐        ┌───────────────┐
     │   5516    │        │    DHT11      │
     │   (ADC)   │        │  (1-Wire)     │
     └─────┬─────┘        └───────┬───────┘
           │                      │
           │    ┌─────────────────┼──────────────┐
           │    │                 │              │
           ▼    ▼                 ▼              ▼
       ┌───────────────────────────────────────────┐
       │               main.c                      │
       │  ① LED翻转 → ② 清屏 → ③ 读取传感器         │
       │  ④ OLED显示 → ⑤ 串口输出 → ⑥ 刷屏 → ⑦ 延时  │
       └──────┬──────────────────────────┬──────────┘
              │                          │
              ▼                          ▼
       ┌───────────┐            ┌─────────────────┐
       │   OLED    │            │     Serial      │
       │  (I2C1)   │            │   (USART3)      │
       └───────────┘            └────────┬────────┘
                                         │
                                         ▼
                                  ┌──────────────────┐
                                  │  Python 上位机    │
                                  │  (PC 端可视化)    │
                                  └──────────────────┘
```

### 依赖关系总结

| 依赖链 | 说明 |
|--------|------|
| `TIM2 → DHT11` | DHT11 时序要求微秒级精度，TIM2 提供 `delay_us()` |
| `I2C1 → OLED` | OLED 驱动通过 I2C1 总线和 Framebuffer 模式实现显示 |
| `ADC1 → 5516` | 5516 光照传感器通过 ADC1_IN0 通道采集模拟电压 |
| `USART3 → Serial → printf` | Serial 模块封装 USART3，实现 `printf` 重定向输出 |
| `RTC → main.c` | 主循环读取 RTC 时间，为串口输出加时间戳 |
| **Python → CSV** | 上位机解析串口数据，用 PC 系统时间戳替代 RTC 存档 |

---

## 6. 数据流向

### 6.1 串联数据流

```
[传感器]
  DHT11 (PA9) ─── 40bit 数字信号 ──→ STM32 解析 buffer[5] → 校验和验证
                                              ↓
  5516 (PA0)  ─── 模拟电压信号 ──→ ADC1 12位采样 (0-4095)
                                              ↓
                                          映射为百分比 (0-100)

[本地显示]
  DHT11/5516 数据 ──→ OLED 显存 (128×8 bytes) ──→ I2C1 刷新 → SSD1306 屏

[串口传输]
  DHT11/5516 + RTC ──→ printf("[YYYY-MM-DD HH:MM:SS] T:xxC, H:xx%, L:xx%")
                     ──→ USART3 (115200-8N1) ──→ PC 串口

[PC 端]
  串口数据 ──→ pyserial line 读取 ──→ 正则解析 (T/H/L)
              │
              ├──→ matplotlib 3子图实时更新（红/绿/蓝三条线）
              │
              └──→ CSV 文件追加（PC时间戳 + T/H/L）
```

### 6.2 数据格式

```
串口输出格式：
[2026-07-21 12:25:45] T:28C, H:47%, L:24%

CSV 记录格式：
Time, Temperature(C), Humidity(%), Light(%)
2026-07-21 12:25:45,28,47,24
2026-07-21 12:25:46,28,47,25
```

### 6.3 时序

```
一次主循环周期（1秒）：
  T0     T0+5ms    T0+30ms    T0+50ms    T0+55ms    T0+65ms   T0+1000ms
  │        │          │          │          │          │          │
  ▼        ▼          ▼          ▼          ▼          ▼          ▼
LED翻转  清屏    读DHT11   读光照ADC  OLED显存  串口输出  HAL_Delay
                               填充      刷I2C     +printf    (等待)
```

---

## 7. 软件架构

### 7.1 分层设计

```
┌───────────────────────────────────────────────────┐
│              应用层 (Application)                   │
│  main.c — 主循环、业务逻辑、功能编排                │
├───────────────────────────────────────────────────┤
│              驱动层 (Drivers/User)                  │
│  DHT11 / 5516 / OLED / LED / Serial                │
│  各传感器协议实现、显示控制、通信封装                │
├───────────────────────────────────────────────────┤
│            HAL 抽象层 (STM32 HAL)                  │
│  stm32f1xx_hal_{adc,gpio,i2c,rtc,tim,uart}.c     │
│  CubeMX 自动生成，屏蔽寄存器级操作                  │
├───────────────────────────────────────────────────┤
│            CMSIS 核心层                            │
│  内核访问、中断向量、系统定时                       │
├───────────────────────────────────────────────────┤
│               硬件层 (Hardware)                    │
│  STM32F103C8T6 + DHT11 + 5516 + OLED + LED        │
└───────────────────────────────────────────────────┘
```

### 7.2 工程组织

```
STM32 固件（C / CMake）
  ├── Core/        — CubeMX 生成的启动、外设初始化代码
  ├── Drivers/     — HAL 库 + CMSIS + 用户自定义驱动
  └── cmake/       — 工具链文件、构建配置

Python 上位机（Python / PyInstaller）
  ├── monitor.py   — 串口读取 + 实时绘图 + CSV 记录
  ├── monitor.spec — PyInstaller 打包配置
  └── .venv/       — Python 虚拟环境（pyserial + matplotlib）
```

### 7.3 构建系统

| 工具 | 版本 | 用途 |
|------|------|------|
| CMake | 3.30+ | 构建管理，Ninja 生成器 |
| ARM GCC | 13.3.1 | 交叉编译 |
| Ninja | 1.13 | 高速构建引擎 |
| OpenOCD | 0.12 | 烧录/调试 |
| pySerial | 3.5 | Python 串口库 |
| matplotlib | 3.11 | Python 绘图库 |

### 7.4 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| **构建系统** | CMake + Ninja | 跨平台、高效增量编译、CubeMX 原生支持 |
| **超时处理** | 100ms 固定超时 | 平衡可靠性与主循环响应性 |
| **CSV 时间戳** | PC 系统时间 | 避免 MCU RTC 掉电归零或 LSI 温漂误差 |
| **波特率** | 115200 | 足够传输速率，USB-Serial 芯片兼容性最好 |
| **OLED 刷新** | 整帧 I2C 传输 | 消除残影，保证显示一致性 |

---

## 8. 项目亮点

### 🏆 亮点一：上下位机协同架构
- STM32 专注于传感器采集和本地显示（实时性强）
- Python 专注于数据可视化和存储（开发效率高）
- 边界清晰、职责分离，可独立迭代

### 🏆 亮点二：全栈工具链自动化
- `install_tools.py` — 一键安装 ARM GCC + CMake + Ninja + OpenOCD
- `flash.bat` — 一键烧录（OpenOCD + ST-Link）
- `启动监测程序.bat` — 一键启动 PC 端监控
- `monitor.spec` — PyInstaller 打包为独立 exe，无需安装 Python

### 🏆 亮点三：精巧的微秒级延时方案
- 使用 TIM2 硬件定时器做 `delay_us()`，精度远高于软件循环
- DHT11 时序要求 20-40μs 精度，软件循环无法保证
- 与其他驱动完全解耦，仅依赖 TIM2 这一通用外设

### 🏆 亮点四：完善的兼容性设计
- **printf 重定向**: 同时支持 GCC (`_write`) 和 ARMCC (`fputc`)，一套代码适配 CubeIDE、CLion、VS Code 任意工具链
- **OLED 地址自检测**: 自动识别 0x78 或 0x7A 两种常见 I2C 地址
- **多层级 Python 路径兜底**: 脚本目录 → 向上搜项目标志 → 已知路径回退

### 🏆 亮点五：专业级数据格式
- 串口输出带 RTC 时间戳（完整 ISO 日期时间格式）
- CSV 文件带 PC 系统时间戳（可离线对比分析）
- 数据校验保护（DHT11 校验和、ADC 数值限幅 0-100）
- 错误恢复（DHT11 读取失败时显示 "Error"，不会卡死主循环）

### 🏆 亮点六：紧凑高效
- Flash 占用仅 40%（26KB/64KB），RAM 占用仅 16%（3.3KB/20KB）
- 预留充足资源供后续扩展（Wi-Fi 模块、蜂鸣器、更多传感器等）
- 干净的架构层分离，扩展新传感器只需在 `Drivers/User/` 中添加驱动

---

*本文档由 Claude Code 于 2026-07-23 自动生成，基于对项目全部源代码和工程结构的系统性分析。*
